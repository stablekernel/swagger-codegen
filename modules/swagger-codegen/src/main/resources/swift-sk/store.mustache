//
// Store.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation

class Store {
    let gateway: Gateway

{{#apiInfo}}
    {{#apis}}
        {{#operations}}
            {{#operation}}
                {{#-last}}
    let {{classname}}: {{classFilename}}StoreProtocol
                {{/-last}}
            {{/operation}}
        {{/operations}}
    {{/apis}}
{{/apiInfo}}

    init(gateway: Gateway,
{{#apiInfo}}
    {{#apis}}
        {{classname}}Store: {{classFilename}}StoreProtocol{{#-last}}) {  {{/-last}}{{^-last}},{{/-last}}
    {{/apis}}
{{/apiInfo}}
        self.gateway = gateway
{{#apiInfo}}
    {{#apis}}
        {{classname}} = {{classname}}Store
    {{/apis}}
{{/apiInfo}}
    }

    convenience init(gateway: Gateway) {
        self.init(gateway: gateway,
{{#apiInfo}}
    {{#apis}}
        {{classname}}Store: {{classFilename}}Store(gateway){{#-last}}){{/-last}}{{^-last}},{{/-last}}
    {{/apis}}
{{/apiInfo}}
    }
}
